query ProgramList(
  $where: ProgramBoolExp
  $orderBy: [ProgramOrderBy!]
  $offset: Int
  $limit: Int
) {
  list: program(
    where: $where
    orderBy: $orderBy
    offset: $offset
    limit: $limit
  ) {
    id
    name
    type
    sponsoringAgency
    dateStart
    dateEnd
  }
  meta: programAggregate(where: $where) {
    aggregate {
      count
    }
  }
}
